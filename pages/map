<style>
    .site_content_container{
        background: none;
    }
</style>
<div class="position_relative page_header margin_bottom_none">
    <div class="page_heading"></div>
</div>
<div class="content_container" style="min-height:600px">
    <h4 class="page_heading2">Centre Map</h4>
    <br />
    <div class="row-fluid">        
        <div class="map">
            <div class="demo1 map3" style="height:650px; width:100%;">            
                <img alt="map_image" id="map_image" src="{{site.property.map_image_path}}" />    			
    			{% for store in stores %}  
    			<div class="marker" id="store_{{store.id}}" data-coords="{{store.x_coordinate | minus:x_offset}}, {{store.y_coordinate | minus:y_offset}}" style="display:none">
    				{{ store.name }}
    			</div>	
    			{% endfor %}
    		</div>
        </div>
    </div>
</div>